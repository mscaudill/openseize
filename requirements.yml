#requirements for the openseize package
name: openseize
dependencies:
    - requests
    - numpy
    - scipy
    - matplotlib
    - ipython
    - notebook
    - pandas
    - pytest
    - scikit-learn
    - psutil
